# Environment Variables for RAG Chatbot API

# Database settings
NEON_DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/dbname?sslmode=require

# API Keys - Only one is required for the migration
COHERE_API_KEY=your_cohere_api_key_here  # Required for Cohere embeddings and text generation

# Optional API Keys (not used after migration)
OPENAI_API_KEY=your_openai_api_key_here   # Optional, for OpenAI embeddings
GOOGLE_API_KEY=your_google_api_key_here   # Optional, not used after migration to Cohere

# Vector Database settings
QDRANT_URL=your_qdrant_url_here           # Optional, if using Qdrant cloud
QDRANT_API_KEY=your_qdrant_api_key_here   # Optional, if using Qdrant cloud

# Application settings
DEBUG=false                               # Set to true for development